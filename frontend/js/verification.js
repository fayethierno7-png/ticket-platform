const f=document.querySelector('#verify'),m=document.querySelector('#msg');f.addEventListener('submit',async e=>{e.preventDefault();const token=sessionStorage.getItem('adminToken');if(!token){location.href='admin.html';return}try{const t=await api('/tickets/verifier',{method:'POST',token,body:JSON.stringify({code:f.code.value.trim()})});const good=t.status==='USED';m.innerHTML=`<p class="notice ${good?'success':'error'}">${good?'✅ Ticket valide — maintenant marqué UTILISÉ.':`❌ Ticket ${t.status==='EXPIRED'?'expiré':t.status==='DISABLED'?'désactivé':'déjà utilisé'}.`}</p>`}catch(e){m.innerHTML=`<p class="notice error">❌ ${e.message}</p>`}});
