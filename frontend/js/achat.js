const form=document.querySelector('#buy'),message=document.querySelector('#message'),total=document.querySelector('#total');form.quantity.addEventListener('input',()=>total.textContent=fmt(5000*Math.max(1,+form.quantity.value||1)));form.addEventListener('submit',async e=>{e.preventDefault();message.innerHTML='';try{const d=Object.fromEntries(new FormData(form));d.quantity=+d.quantity;const tickets=await api('/tickets/acheter',{method:'POST',body:JSON.stringify(d)});sessionStorage.setItem('lastTickets',JSON.stringify(tickets));location.href='ticket.html'}catch(e){message.innerHTML=`<p class="notice error">${e.message}</p>`}});
